const chai = require('chai');
const { oauth, login } = require('../Utils');

const { expect } = chai;

// describe('output global user', function () {
//   before(function () {
//     return login(oauth);
//   });
//   it('should get global user info', function () {
//     expect(global.user_token).to.be.an('object');
//   });
// });
