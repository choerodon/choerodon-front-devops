const TASK_TYPE_DEPLOY = 'auto';
const TASK_TYPE_MANUAL = 'manual';

const MODE_TYPE_NEW = 'new';
const MODE_TYPE_UPDATE = 'update';

const VERSION_TYPE = ['master', 'feature', 'bugfix', 'release', 'hotfix'];

const AUDIT_MODE_SING = 'sign';
const AUDIT_MODE_ORSING = 'orSign';

const TASK_PARALLEL = 'parallel';
const TASK_SERIAL = 'serial';

export {
  TASK_TYPE_DEPLOY,
  TASK_TYPE_MANUAL,
  MODE_TYPE_NEW,
  MODE_TYPE_UPDATE,
  VERSION_TYPE,
  AUDIT_MODE_ORSING,
  AUDIT_MODE_SING,
  TASK_PARALLEL,
  TASK_SERIAL,
};
